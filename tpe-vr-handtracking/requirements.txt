# ============================================================
#  Dépendances du projet — versions ÉPINGLÉES pour reproductibilité
#  Python cible : 3.10.x  (voir .python-version)
#  Installation : pip install -r requirements.txt
# ============================================================

# --- Vision par ordinateur & IA ---
opencv-python==4.9.0.80        # Capture webcam + affichage overlay
mediapipe==0.10.9              # Détection de main en temps réel (Google)

# --- Moteur 3D ---
ursina==5.3.0                  # Rendu 3D Python (basé sur Panda3D)

# --- Tests ---
pytest==8.2.0                  # Framework de tests unitaires
pytest-mock==3.14.0            # Mocking pour les tests de vision

# --- Outils dev (optionnel, non installé en prod CI) ---
# black==24.4.2                # Formatage automatique du code
# flake8==7.0.0                # Linting
